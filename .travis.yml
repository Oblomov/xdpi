os:
 - linux

dist: trusty
sudo: false
addons:
 apt:
  sources:
   - sourceline: "deb http://archive.ubuntu.com/ubuntu trusty universe"
  packages:
   - libxinerama-dev
   - libxrandr-dev
   - libxcb-xinerama0-dev
   - libxcb-randr0-dev
   - libxcb-xrm-dev

language: c
compiler:
 - gcc
 - clang

script: make && ./xdpi
